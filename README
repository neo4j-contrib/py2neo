py2neo - python library to access neo4j via REST interface

more documentation to come, still working it out myself :-)

---

import py2neo as neo

gdb = neo.GraphDatabaseService("http://localhost:7474/db/data")
ref_node = gdb.get_reference_node()
traverser = ref_node.get_node_traverser()
for node in traverser.traverse_depth_first():
	print node

